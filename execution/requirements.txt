# Lemon Screenplay Ingestion — Python Dependencies
# Install: pip install -r execution/requirements.txt

# ── Core HTTP ─────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── PDF Parsing (used by parse_screenplay_pdf_v2.py) ─────────────────────────
PyPDF2>=3.0.0
pdfplumber>=0.10.0
pymupdf>=1.23.0          # fitz — best native PDF extraction

# ── OCR support (optional — for scanned PDFs) ─────────────────────────────────
# Uncomment if you need OCR for scanned/image PDFs:
# pytesseract>=0.3.10
# pdf2image>=1.16.3

# ── Firebase Admin SDK (Firestore + Storage writes) ───────────────────────────
firebase-admin>=6.4.0

# ── Retry / backoff (used by network_utils.py) ────────────────────────────────
tenacity>=8.2.0
