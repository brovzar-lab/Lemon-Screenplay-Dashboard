# ===================================================================
# Lemon Screenplay Dashboard — Environment Variables
# ===================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ===================================================================

# --- Firebase Storage ---
# Your Firebase Storage bucket name
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app

# --- Anthropic API Key ---
# Used by the dev proxy for local screenplay analysis
# Production uses the Cloud Function proxy (no browser key needed)
VITE_ANTHROPIC_API_KEY=sk-ant-...

# --- Google API Key ---
# For Gemini-powered features: poster generation + DevExec AI chat
# Optional — these features degrade gracefully without it
VITE_GOOGLE_API_KEY=your-google-api-key

# ===================================================================
# VPS Daemon Environment (set in systemd unit, NOT in this file)
# ===================================================================
# ANTHROPIC_API_KEY          — Anthropic key for V9 analysis engine
# FIREBASE_PROJECT_ID        — Firebase project ID
# GOOGLE_APPLICATION_CREDENTIALS — Path to service-account.json
# TMDB_API_KEY               — TMDB API key for produced-film detection
