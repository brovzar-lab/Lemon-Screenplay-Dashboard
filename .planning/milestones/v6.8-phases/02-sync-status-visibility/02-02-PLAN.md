---
phase: 02-sync-status-visibility
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/SyncStatusIndicator.tsx
  - src/components/layout/SyncStatusIndicator.test.tsx
  - src/hooks/useSyncRetry.ts
  - src/hooks/useSyncRetry.test.ts
  - src/components/layout/Header.tsx
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02

must_haves:
  truths:
    - "Producer sees a pending count badge in the header when screenplays are awaiting Firestore sync"
    - "Producer sees a Retry Now button when writes have failed"
    - "Clicking Retry Now triggers flushPendingWrites and updates the count"
    - "Indicator is invisible when all screenplays are synced (pendingCount === 0 and not retrying)"
    - "After successful retry the count decrements and failure indicator clears"
  artifacts:
    - path: "src/components/layout/SyncStatusIndicator.tsx"
      provides: "Conditional badge with pending count and retry button"
      exports: ["SyncStatusIndicator"]
    - path: "src/hooks/useSyncRetry.ts"
      provides: "Hook wrapping retry logic with loading/error state"
      exports: ["useSyncRetry"]
    - path: "src/components/layout/SyncStatusIndicator.test.tsx"
      provides: "Component render tests for indicator states"
      contains: "describe.*SyncStatusIndicator"
    - path: "src/hooks/useSyncRetry.test.ts"
      provides: "Hook tests for retry logic"
      contains: "describe.*useSyncRetry"
    - path: "src/components/layout/Header.tsx"
      provides: "Header with SyncStatusIndicator mounted"
      contains: "SyncStatusIndicator"
  key_links:
    - from: "src/components/layout/SyncStatusIndicator.tsx"
      to: "src/stores/syncStatusStore.ts"
      via: "useSyncStatusStore hook"
      pattern: "useSyncStatusStore"
    - from: "src/components/layout/SyncStatusIndicator.tsx"
      to: "src/hooks/useSyncRetry.ts"
      via: "useSyncRetry hook for retry button"
      pattern: "useSyncRetry"
    - from: "src/hooks/useSyncRetry.ts"
      to: "src/lib/analysisStore.ts"
      via: "import flushPendingWrites and getPendingWriteCount"
      pattern: "import.*flushPendingWrites.*analysisStore"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/SyncStatusIndicator.tsx"
      via: "component mount in stats pills area"
      pattern: "<SyncStatusIndicator"
---

<objective>
Build the SyncStatusIndicator component, useSyncRetry hook, wire into Header, and verify visually.

Purpose: This is the user-facing layer — the producer sees pending sync count and can retry failed writes from the header.
Output: Working indicator in the Header, retry hook, component and hook tests, visual verification.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sync-status-visibility/02-RESEARCH.md
@.planning/phases/02-sync-status-visibility/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/stores/syncStatusStore.ts (created in Plan 01):
```typescript
interface SyncStatusState {
  pendingCount: number;
  isRetrying: boolean;
  lastRetryError: string | null;
  setPendingCount: (count: number) => void;
  setRetrying: (retrying: boolean) => void;
  setLastRetryError: (error: string | null) => void;
}

export const useSyncStatusStore: UseBoundStore<StoreApi<SyncStatusState>>;
export function startSyncStatusPolling(): () => void;
```

From src/lib/analysisStore.ts (exports added in Plan 01):
```typescript
export function getPendingWriteCount(): number;
export async function flushPendingWrites(): Promise<void>;
```

From src/components/layout/Header.tsx (current structure):
```typescript
// Stats pills section (line 59-84) — SyncStatusIndicator goes between StatPills and DevExecToggle
// The indicator should be placed after the StatPill elements, before the DevExecToggle (line 87)
export function Header(): JSX.Element;
```

Visual conventions (from Header.tsx):
- StatPill uses: `glass` bg, `text-black-400` labels, `text-gold-400` values
- Pill sizing: `px-4 py-2 rounded-lg`
- Amber color for warnings: `bg-amber-500/10 border-amber-500/20 text-amber-400`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useSyncRetry hook with tests</name>
  <files>src/hooks/useSyncRetry.ts, src/hooks/useSyncRetry.test.ts</files>
  <behavior>
    - retryAll() calls flushPendingWrites then refreshes pendingCount
    - retryAll() sets isRetrying=true before flush, false after
    - retryAll() clears lastRetryError on start
    - retryAll() sets lastRetryError if flushPendingWrites throws
    - retryAll() no-ops if already retrying (prevents concurrent flushes)
  </behavior>
  <action>
    1. Create `src/hooks/useSyncRetry.ts`:
       - Import `useSyncStatusStore` from `@/stores/syncStatusStore`
       - Import `flushPendingWrites`, `getPendingWriteCount` from `@/lib/analysisStore`
       - Export `useSyncRetry` hook that returns `{ retryAll }` where retryAll is an async function:
         - Guard: if `useSyncStatusStore.getState().isRetrying` is true, return early (prevent concurrent retries — Pitfall 2 from RESEARCH.md)
         - Set `isRetrying(true)`, `lastRetryError(null)`
         - try: `await flushPendingWrites()`, then `setPendingCount(getPendingWriteCount())` (immediate refresh — Pitfall 4 from RESEARCH.md)
         - catch: `setLastRetryError(err.message)`
         - finally: `setRetrying(false)`
       - Use `useCallback` to memoize `retryAll`

    2. Create `src/hooks/useSyncRetry.test.ts`:
       - Mock `@/lib/analysisStore` (flushPendingWrites, getPendingWriteCount)
       - Test: retryAll calls flushPendingWrites
       - Test: retryAll sets isRetrying true then false
       - Test: retryAll refreshes pendingCount after flush
       - Test: retryAll sets lastRetryError on failure
       - Test: retryAll no-ops when already retrying
       - Use `renderHook` from `@testing-library/react` and `act` for async
  </action>
  <verify>
    <automated>npm run test:run -- src/hooks/useSyncRetry.test.ts</automated>
  </verify>
  <done>useSyncRetry hook exports retryAll. All tests pass including concurrent retry guard and error handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SyncStatusIndicator component with tests, wire into Header</name>
  <files>src/components/layout/SyncStatusIndicator.tsx, src/components/layout/SyncStatusIndicator.test.tsx, src/components/layout/Header.tsx</files>
  <behavior>
    - Renders nothing when pendingCount=0 and isRetrying=false
    - Shows "{N} pending" text when pendingCount > 0
    - Shows "Retry Now" button when pendingCount > 0 and not retrying
    - Shows "Syncing..." text when isRetrying=true
    - Hides "Retry Now" button when isRetrying=true
    - Clicking "Retry Now" calls retryAll
    - Shows error message when lastRetryError is set
    - Starts polling on mount via useEffect calling startSyncStatusPolling
    - Cleans up polling interval on unmount
  </behavior>
  <action>
    1. Create `src/components/layout/SyncStatusIndicator.tsx`:
       - Import `useSyncStatusStore`, `startSyncStatusPolling` from `@/stores/syncStatusStore`
       - Import `useSyncRetry` from `@/hooks/useSyncRetry`
       - useEffect on mount: call `startSyncStatusPolling()`, return cleanup function
       - Render logic:
         - If `pendingCount === 0 && !isRetrying && !lastRetryError` → return `null`
         - Container: `flex items-center gap-2 px-3 py-1.5 rounded-lg bg-amber-500/10 border border-amber-500/20` (matches premium amber warning theme)
         - Show sync icon (small rotating SVG circle arrow when retrying, static when not)
         - Text: `isRetrying ? 'Syncing...' : \`${pendingCount} pending\``  — `text-sm text-amber-400`
         - Retry button: shown when `!isRetrying && pendingCount > 0` — `text-xs px-2 py-0.5 rounded bg-amber-500/20 hover:bg-amber-500/30 text-amber-300 transition-colors`
         - Error text: if `lastRetryError` — small red text below or inline
       - Export as named export `SyncStatusIndicator`

    2. Create `src/components/layout/SyncStatusIndicator.test.tsx`:
       - Mock `@/stores/syncStatusStore` and `@/hooks/useSyncRetry`
       - Test: renders nothing when pendingCount=0, isRetrying=false, lastRetryError=null
       - Test: renders pending count when pendingCount > 0
       - Test: renders Retry Now button when pendingCount > 0 and not retrying
       - Test: Retry Now button calls retryAll on click
       - Test: renders "Syncing..." when isRetrying=true
       - Test: hides Retry Now button when isRetrying=true
       - Test: renders error message when lastRetryError is set
       - Test: calls startSyncStatusPolling on mount
       - Use `render` from `@testing-library/react`, `screen`, `fireEvent`

    3. Modify `src/components/layout/Header.tsx`:
       - Add import: `import { SyncStatusIndicator } from './SyncStatusIndicator';`
       - Insert `<SyncStatusIndicator />` after the StatPill elements block (after line 83, before the DevExecToggle on line 87)
       - Place it inside the existing `flex flex-wrap items-center gap-3` container so it aligns with stat pills
  </action>
  <verify>
    <automated>npm run test:run -- src/components/layout/SyncStatusIndicator.test.tsx</automated>
  </verify>
  <done>SyncStatusIndicator renders conditionally in Header. Shows count + retry button when pending > 0. Hidden when synced. All component tests pass. npm run build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of sync status indicator</name>
  <action>Human verifies the sync status indicator appearance and behavior in the running app.</action>
  <verify>Human confirms indicator appears/disappears correctly and matches theme.</verify>
  <done>Visual and functional behavior approved by user.</done>
  <what-built>Sync status indicator in the dashboard header showing pending Firestore write count with a manual retry button. The indicator appears only when writes are pending or retrying, and vanishes when all synced.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Verify the header loads normally — no sync indicator visible (no pending writes)
    3. To test the indicator, open browser DevTools console and run:
       `localStorage.setItem('lemon-pending-writes', JSON.stringify([{source_file: 'test.pdf'}]))`
       Wait 2 seconds (polling interval) — an amber "1 pending" badge should appear in the header
    4. Click "Retry Now" — should show "Syncing..." briefly, then either clear (if Firestore write succeeds) or show an error
    5. Clear the test data: `localStorage.removeItem('lemon-pending-writes')` — indicator should vanish after 2 seconds
    6. Verify the indicator matches the premium gold/black theme (amber tones, not jarring)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run test:run` — all tests pass (store, hook, component)
2. `npm run build` — no TypeScript errors
3. Visual: indicator appears only when pending > 0, retry works, clears on success
</verification>

<success_criteria>
- SyncStatusIndicator shows pending count in header when writes are queued (SYNC-01)
- Retry Now button triggers flushPendingWrites and refreshes count (SYNC-02)
- Indicator is invisible during normal synced operation (no visual noise)
- After successful retry, count decrements and failure state clears
- All unit tests pass, build succeeds
- Human verifies visual appearance and functional behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-sync-status-visibility/02-02-SUMMARY.md`
</output>
