---
phase: 01-firestore-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/analysisStore.ts
  - src/lib/analysisStore.test.ts
  - firestore.rules
autonomous: true
requirements: []

must_haves:
  truths:
    - "All Firestore reads and writes in analysisStore.ts are gated behind authReady"
    - "Firestore rules require request.auth != null for all internal collection reads and writes"
    - "shared_views collection rule is pre-stubbed with write guard for Phase 5"
    - "Storage rules remain publicly readable (no change)"
    - "Unit tests confirm backgroundFirestoreSync and saveAnalysis await authReady"
  artifacts:
    - path: "src/lib/analysisStore.ts"
      provides: "authReady gate before all Firestore SDK calls"
      contains: "await authReady"
    - path: "src/lib/analysisStore.test.ts"
      provides: "Tests confirming authReady is awaited before Firestore calls"
    - path: "firestore.rules"
      provides: "Tightened rules: request.auth != null on all collections"
      contains: "request.auth != null"
  key_links:
    - from: "src/lib/analysisStore.ts"
      to: "src/lib/firebase.ts"
      via: "import { authReady } from './firebase'"
      pattern: "await authReady"
    - from: "firestore.rules"
      to: "uploaded_analyses"
      via: "allow read: if request.auth != null"
      pattern: "request.auth != null"
---

<objective>
Gate all Firestore calls in analysisStore.ts behind the authReady promise, then tighten Firestore security rules so unauthenticated clients receive PERMISSION_DENIED on all internal collections.

Purpose: The deployment order is deliberate — client auth gate ships first (Plan 01), then rules are tightened here (Plan 02). This eliminates the window where rules are live but the client cannot authenticate. The rules file is deployed via Firebase CLI after both code changes are verified locally.

Output: Updated analysisStore.ts with authReady gates, updated analysisStore.test.ts confirming the gates, and a rewritten firestore.rules with auth guards on all collections plus a pre-stubbed shared_views rule.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-firestore-security-hardening/01-01-SUMMARY.md
@src/lib/analysisStore.ts
@firestore.rules
</context>

<interfaces>
<!-- authReady import contract from Plan 01 output -->

From src/lib/firebase.ts (created in Plan 01):
```typescript
import type { User } from 'firebase/auth';

export const auth: Auth;
export const authReady: Promise<User>;  // resolves once anonymous session established
export const db: Firestore;
export const storage: FirebaseStorage;
```

Firestore call sites in analysisStore.ts that need authReady gate:
- backgroundFirestoreSync() — calls getDocs, setDoc (line ~134)
- saveAnalysis() — calls setDoc (line ~207)
- removeAnalysis() — calls deleteDoc (line ~258)
- clearAllAnalyses() — calls getDocs + deleteDoc (line ~279)
- getAnalysisCount() — calls getCountFromServer (line ~298)
- removeMultipleAnalyses() — calls deleteDoc (line ~320)
- flushPendingWrites() — calls setDoc (line ~94, called only from backgroundFirestoreSync)

localStorage call sites (do NOT add authReady gate — these are synchronous, no auth needed):
- readFromLocal(), writeToLocal(), queueForRetry()
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate Firestore calls in analysisStore.ts behind authReady</name>
  <files>src/lib/analysisStore.ts, src/lib/analysisStore.test.ts</files>
  <behavior>
    - Test: backgroundFirestoreSync awaits authReady before calling getDocs (mock authReady and assert it resolves before getDocs is called)
    - Test: saveAnalysis awaits authReady before calling setDoc on the Firestore path (localStorage write must NOT be blocked by authReady — it fires immediately)
    - Test: removeAnalysis awaits authReady before calling deleteDoc
    - Test: clearAllAnalyses awaits authReady before calling getDocs
    - Mock strategy: vi.mock('./firebase', () => ({ authReady: Promise.resolve(), db: mockDb })); vi.mock('firebase/firestore', ...) to stub getDocs, setDoc, deleteDoc, etc.
  </behavior>
  <action>
    Write/update analysisStore.test.ts FIRST covering the authReady gate assertions, then add gates to analysisStore.ts to make them pass.

    analysisStore.ts changes — add `await authReady` as the FIRST line inside the Firestore section of each function:

    1. backgroundFirestoreSync(): add `await authReady;` as the very first statement (before flushPendingWrites call). This gates the entire background sync chain since flushPendingWrites is only called from here.

    2. saveAnalysis(): the function has two sections — localStorage (immediate) and Firestore (async). Add `await authReady;` immediately before `const docId = toDocId(sourceFile);` in the Firestore section. The localStorage write at the top of the function must NOT be moved or gated.

    3. removeAnalysis(): add `await authReady;` before `const docId = toDocId(sourceFile);` in the Firestore section (after the localStorage removal).

    4. clearAllAnalyses(): add `await authReady;` before the Firestore try block (after localStorage removal).

    5. getAnalysisCount(): add `await authReady;` before the `getCountFromServer` call in the fallback path (the localStorage fast path at top returns without Firestore — no gate needed there).

    6. removeMultipleAnalyses(): add `await authReady;` before the first Firestore batchSize loop (after localStorage removal).

    Import to add at top of analysisStore.ts:
    ```typescript
    import { authReady } from './firebase';
    ```

    IMPORTANT: localStorage operations (readFromLocal, writeToLocal, queueForRetry) must remain synchronous and ungated. Only Firestore SDK calls get the authReady gate. This preserves the fast-path localStorage return in loadAllAnalyses.

    IMPORTANT: flushPendingWrites() calls setDoc internally but is only called from backgroundFirestoreSync() which already awaits authReady at the top. Do NOT add a second authReady gate inside flushPendingWrites — it would be redundant and the research explicitly confirmed this.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/LEMON-SCREENPLAY-DASHBOARD && npm run test:run -- src/lib/analysisStore.test.ts</automated>
  </verify>
  <done>analysisStore.test.ts passes. All Firestore call paths await authReady. localStorage paths are unchanged and not gated. npm run build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite firestore.rules with auth guards + shared_views stub</name>
  <files>firestore.rules</files>
  <action>
    Rewrite firestore.rules. Preserve all existing helper functions (onlyHasKeys, validString) and all existing write validation logic. Only change read/write guards.

    Changes per collection:

    uploaded_analyses:
    - `allow read: if true;` → `allow read: if request.auth != null;`
    - `allow write: if request.resource.data.keys().hasAny([...])` → `allow write: if request.auth != null && request.resource.data.keys().hasAny([...])`
    - Preserve the exact source_file string length check unchanged

    screenplay_feedback:
    - `allow read: if true;` → `allow read: if request.auth != null;`
    - `allow write: if 'screenplayId' in ...` → `allow write: if request.auth != null && 'screenplayId' in ...`
    - Preserve the screenplayId and text field validation unchanged
    - Add comment: `// Intentionally readable by any authenticated session — producer notes are visible in shared views`

    producer_profiles:
    - `allow read: if true;` → `allow read: if request.auth != null;`
    - `allow write: if ...` → `allow write: if request.auth != null && ...`
    - Preserve the name field validation unchanged

    Add shared_views collection stub BEFORE the catch-all deny block:
    ```
    // ── shared_views (Phase 5 creates this collection) ────────────────────────
    // Token = capability: anyone with the share URL may read the document.
    // Only authenticated sessions (the dashboard) may create or revoke tokens.
    // Phase 5 will refine the read rule if parameterized token lookup is needed.
    match /shared_views/{tokenId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    ```

    Update the file header comment to reflect the new security model:
    - Remove the TODO about enabling App Check (App Check is explicitly skipped — anonymous auth is the chosen mechanism)
    - Update CURRENT MODE comment: "Internal tool — anonymous auth required for internal collections; shared_views readable by token URL"

    DO NOT touch storage.rules — Storage stays publicly readable per user decision.

    After writing the file, deploy rules:
    ```bash
    firebase deploy --only firestore:rules
    ```

    If deploy fails with auth error, the executor should pause and ask the user to run `firebase login` then retry the deploy command.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/LEMON-SCREENPLAY-DASHBOARD && npm run build && npm run test:run</automated>
  </verify>
  <done>firestore.rules deployed successfully. npm run build and npm run test:run both exit 0. Rules file contains `request.auth != null` guards on all three internal collections and the shared_views stub.</done>
</task>

</tasks>

<verification>
After both tasks:
1. analysisStore.ts imports authReady and awaits it before every Firestore SDK call
2. localStorage paths in analysisStore.ts are NOT gated (readFromLocal, writeToLocal stay synchronous)
3. firestore.rules: uploaded_analyses, screenplay_feedback, producer_profiles all require `request.auth != null` for reads and writes
4. firestore.rules: shared_views stub present with `allow read: if true` and `allow write: if request.auth != null`
5. firestore.rules deployed to Firebase (not just updated locally)
6. storage.rules unchanged
7. npm run build exits 0
8. npm run test:run exits 0
</verification>

<success_criteria>
- All Firestore reads and writes in analysisStore.ts are gated on authReady
- firestore.rules deployed with auth guards on all internal collections
- shared_views pre-stubbed for Phase 5
- Unit tests confirm authReady gate behavior
- Build and full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/01-firestore-security-hardening/01-02-SUMMARY.md`
</output>
