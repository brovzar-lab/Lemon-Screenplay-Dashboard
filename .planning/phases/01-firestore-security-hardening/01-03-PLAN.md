---
phase: 01-firestore-security-hardening
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Dashboard loads and displays screenplays normally after auth and rules changes"
    - "Browser console shows no PERMISSION_DENIED errors during normal operation"
    - "Unauthenticated Firestore reads return PERMISSION_DENIED (confirmed in console smoke test)"
    - "Anonymous uid is stable across page refreshes (same uid on reload)"
  artifacts:
    - path: "src/lib/firebase.ts"
      provides: "Auth initialized — verified working in production-deployed app"
    - path: "firestore.rules"
      provides: "Deployed rules — verified blocking unauthenticated access"
  key_links:
    - from: "dashboard load"
      to: "Firestore screenplays"
      via: "anonymous auth → authReady → getDocs"
      pattern: "screenplays load without PERMISSION_DENIED"
---

<objective>
Verify the deployed auth + rules changes work end-to-end: dashboard loads normally, screenplays appear, and an unauthenticated console probe returns PERMISSION_DENIED.

Purpose: Rules changes take effect immediately on deploy. This checkpoint catches any auth-Firestore race or misconfiguration before the milestone continues. It is the production gate for Phase 1.

Output: Human confirmation that the dashboard works correctly under the new security model.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-firestore-security-hardening/01-01-SUMMARY.md
@.planning/phases/01-firestore-security-hardening/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy client code to production</name>
  <files></files>
  <action>
    Deploy the updated client code (firebase.ts with authReady, analysisStore.ts with auth gates) to Firebase Hosting so the verification checkpoint tests against production, not localhost.

    Run:
    ```bash
    npm run deploy
    ```

    npm run deploy runs: npm run build (TypeScript check + Vite build) then firebase deploy.

    If the deploy fails:
    - TypeScript error: fix the type error reported by the build step
    - Firebase CLI auth error: run `firebase login` then retry
    - Build size error: unlikely given the small change, but check bundle output

    After successful deploy, note the deployment URL from the firebase deploy output (typically https://lemon-screenplay-dashboard.web.app or similar).
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/LEMON-SCREENPLAY-DASHBOARD && npm run deploy</automated>
  </verify>
  <done>npm run deploy exits 0. Firebase Hosting deployment URL is live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify auth and rules in production</name>
  <files></files>
  <action>Human verification of deployed auth + rules changes against the production URL.</action>
  <verify>
    <automated>MISSING — human verification required; no automated command can substitute for browser console confirmation</automated>
  </verify>
  <done>Human confirms all four checks pass: screenplays load, no PERMISSION_DENIED on normal load, unauthenticated probe blocked, no errors on refresh.</done>
  <what-built>
    Firebase Anonymous Authentication is now active. The dashboard silently creates an anonymous session on load (no login screen). Firestore rules now require request.auth != null for all internal collection reads and writes. Rules are deployed to production.
  </what-built>
  <how-to-verify>
    1. Open the deployed app in a browser (use the URL from the deploy output, e.g. https://lemon-screenplay-dashboard.web.app)

    2. Open DevTools → Console. Confirm NO "PERMISSION_DENIED" errors appear during normal load.

    3. Confirm screenplays load in the grid as they did before this change.

    4. Alternative auth check: look for `[Lemon] Sync complete` or `[Lemon] Loaded X analyses from localStorage` in the console — these only appear if the Firestore auth path succeeded without errors.

    5. UNAUTHENTICATED PROBE — In a NEW incognito window, open DevTools Console and paste:
       ```javascript
       fetch('https://firestore.googleapis.com/v1/projects/lemon-screenplay-dashboard/databases/(default)/documents/uploaded_analyses')
         .then(r => r.json())
         .then(console.log)
       ```
       Expected result: Either a 403/PERMISSION_DENIED response, or an error object — NOT a list of screenplay documents.

    6. Refresh the dashboard in the main (non-incognito) window. Open Console and confirm there are no new errors on refresh. The anonymous session should be restored from localStorage (browserLocalPersistence).

    Confirm all of the following are TRUE:
    - [ ] Screenplays load normally with no console errors
    - [ ] No PERMISSION_DENIED errors in the console during normal load
    - [ ] The unauthenticated probe does NOT return screenplay data
    - [ ] No errors on page refresh
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four checks pass.
    Describe any failures — e.g. "PERMISSION_DENIED on normal load" or "screenplays not loading" — and Claude will diagnose.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when the human verification checkpoint passes:
- Dashboard loads and displays screenplays without errors
- No PERMISSION_DENIED in console during authenticated operation
- Unauthenticated probe cannot read uploaded_analyses
- Page refresh produces no errors
</verification>

<success_criteria>
- Production deployment live with anonymous auth + tightened rules
- Human confirms dashboard functionality unchanged from user perspective
- Human confirms unauthenticated access is blocked
- Phase 1 security goal achieved: internal collections protected before any share link can be generated
</success_criteria>

<output>
After completion, create `.planning/phases/01-firestore-security-hardening/01-03-SUMMARY.md`
</output>
