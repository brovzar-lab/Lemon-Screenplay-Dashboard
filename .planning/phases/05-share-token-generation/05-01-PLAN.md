---
phase: 05-share-token-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shareService.ts
  - src/lib/shareService.test.ts
  - src/stores/shareStore.ts
  - src/stores/shareStore.test.ts
  - src/lib/analysisStore.ts
autonomous: true
requirements:
  - SHARE-01

must_haves:
  truths:
    - "createShareToken writes a shared_views doc to Firestore and returns a URL with the token"
    - "getExistingShareToken finds an existing token for a screenplay or returns null"
    - "revokeShareToken deletes the shared_views doc from Firestore"
    - "isScreenplaySynced checks Firestore for the screenplay doc before sharing"
    - "shareStore caches active tokens keyed by screenplayId for the session"
  artifacts:
    - path: "src/lib/shareService.ts"
      provides: "Firestore CRUD for shared_views collection"
      exports: ["createShareToken", "revokeShareToken", "getExistingShareToken", "getAllSharedViews", "isScreenplaySynced", "SharedView"]
    - path: "src/lib/shareService.test.ts"
      provides: "Unit tests for shareService functions"
      min_lines: 40
    - path: "src/stores/shareStore.ts"
      provides: "Ephemeral Zustand store caching active share tokens per session"
      exports: ["useShareStore"]
    - path: "src/stores/shareStore.test.ts"
      provides: "Unit tests for shareStore"
      min_lines: 20
  key_links:
    - from: "src/lib/shareService.ts"
      to: "src/lib/firebase.ts"
      via: "authReady gate + db import"
      pattern: "await authReady"
    - from: "src/lib/shareService.ts"
      to: "src/lib/analysisStore.ts"
      via: "toDocId import for sync check"
      pattern: "import.*toDocId.*from.*analysisStore"
---

<objective>
Build the share service layer and session cache store for per-screenplay share token generation.

Purpose: Provides the async Firestore CRUD operations (create, revoke, lookup, sync-check) and a Zustand session cache that the UI layer (Plan 02) will consume. Exporting `toDocId` from analysisStore enables the sync pre-check.
Output: `shareService.ts`, `shareStore.ts`, their tests, and `toDocId` exported from `analysisStore.ts`
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-share-token-generation/05-CONTEXT.md
@.planning/phases/05-share-token-generation/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/firebase.ts:
```typescript
export const db: Firestore;
export const storage: FirebaseStorage;
export const auth: Auth;
export const authReady: Promise<User>;
```

From src/lib/analysisStore.ts (line 35, currently NOT exported — Task 1 will export it):
```typescript
function toDocId(sourceFile: string): string {
  // Sanitizes sourceFile into a valid Firestore document ID
  return sourceFile.replace(/[\/\.\#\$\[\]]/g, '_');
}
```

From src/stores/toastStore.ts (pattern reference for ephemeral store):
```typescript
export const useToastStore = create<ToastStore>((set, get) => ({
  toasts: [],
  addToast: (message, severity?) => { ... },
  removeToast: (id) => { ... },
  clearToasts: () => set({ toasts: [] }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shareService.ts and shareStore.ts with tests</name>
  <files>src/lib/shareService.ts, src/lib/shareService.test.ts, src/stores/shareStore.ts, src/stores/shareStore.test.ts</files>
  <behavior>
    - createShareToken(screenplayId, title, includeNotes) writes a doc to `shared_views` collection with crypto.randomUUID() token, returns { token, url }
    - getExistingShareToken(screenplayId) returns SharedView if a doc with matching screenplayId exists, null otherwise
    - revokeShareToken(token) deletes the doc from Firestore AND calls useShareStore.getState().removeToken() to clear cache
    - getAllSharedViews() returns all SharedView docs from the collection
    - isScreenplaySynced(sourceFile) checks if a doc exists in `uploaded_analyses` using toDocId()
    - shareStore.setToken(screenplayId, view) adds to tokens map
    - shareStore.removeToken(screenplayId) removes from tokens map
    - shareStore.clearAll() empties the tokens map
  </behavior>
  <action>
    **Step 1 — Export toDocId:** In `src/lib/analysisStore.ts`, change `function toDocId` to `export function toDocId` (line 35). No other changes to this file.

    **Step 2 — Create shareService.ts** at `src/lib/shareService.ts`:
    - Import `doc, setDoc, deleteDoc, getDoc, getDocs, collection, query, where` from `firebase/firestore`
    - Import `authReady, db` from `./firebase`
    - Import `toDocId` from `./analysisStore`
    - Define and export `SharedView` interface: `{ token: string; screenplayId: string; screenplayTitle: string; includeNotes: boolean; createdAt: string }`
    - Define `SHARED_VIEWS_COLLECTION = 'shared_views'` and `SHARE_BASE_URL = 'https://lemon-screenplay-dashboard.web.app/share'`
    - Implement `createShareToken(screenplayId, screenplayTitle, includeNotes)`: await authReady, generate token via `crypto.randomUUID()`, setDoc with token as doc ID, return `{ token, url }`
    - Implement `revokeShareToken(token)`: await authReady, deleteDoc, then call `useShareStore.getState().removeToken()` for the associated screenplayId (query first to find screenplayId, or accept screenplayId as param). Per research pitfall 3, centralize cache invalidation here. Simplest approach: accept both `token` and `screenplayId` as params so the caller provides both.
    - Implement `getExistingShareToken(screenplayId)`: await authReady, query where screenplayId == screenplayId, return first doc or null
    - Implement `getAllSharedViews()`: await authReady, getDocs on entire collection, map to SharedView[]
    - Implement `isScreenplaySynced(sourceFile)`: await authReady, getDoc on `uploaded_analyses` using `toDocId(sourceFile)`, return `snapshot.exists()`

    **Step 3 — Create shareStore.ts** at `src/stores/shareStore.ts`:
    - Ephemeral Zustand store (no persist middleware), matching toastStore/syncStatusStore pattern
    - State: `tokens: Record<string, SharedView>` (keyed by screenplayId)
    - Actions: `setToken(screenplayId, view)`, `removeToken(screenplayId)`, `clearAll()`

    **Step 4 — Create tests:**
    - `shareService.test.ts`: Mock `firebase/firestore` functions and `./firebase` module. Test each exported function: createShareToken writes correct doc shape, revokeShareToken calls deleteDoc, getExistingShareToken returns data or null, isScreenplaySynced returns boolean based on snapshot.exists(). Mock `crypto.randomUUID` to return a predictable value.
    - `shareStore.test.ts`: Pure Zustand tests (no mocks needed). Test setToken adds entry, removeToken removes entry, clearAll empties map.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/LEMON-SCREENPLAY-DASHBOARD && npm run test:run -- src/lib/shareService.test.ts src/stores/shareStore.test.ts</automated>
  </verify>
  <done>
    - shareService.ts exports createShareToken, revokeShareToken, getExistingShareToken, getAllSharedViews, isScreenplaySynced, SharedView
    - shareStore.ts exports useShareStore with setToken, removeToken, clearAll
    - toDocId is exported from analysisStore.ts
    - All tests pass
    - npm run build succeeds (no TypeScript errors)
  </done>
</task>

</tasks>

<verification>
```bash
npm run test:run -- src/lib/shareService.test.ts src/stores/shareStore.test.ts
npm run build
```
</verification>

<success_criteria>
- shareService.ts provides complete Firestore CRUD for shared_views collection
- shareStore.ts provides ephemeral session cache for active tokens
- toDocId exported from analysisStore for reuse
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-share-token-generation/05-01-SUMMARY.md`
</output>
