---
phase: 07-export-coverage-package
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/export/CoverageDocument.tsx
autonomous: false
requirements:
  - EXPORT-01
gap_closure: true

must_haves:
  truths:
    - "Cover page title and author name render without any visual overlap"
    - "22pt title heading has comfortable whitespace below it before the 11pt author line"
  artifacts:
    - path: "src/components/export/CoverageDocument.tsx"
      provides: "Cover page layout with corrected title/author spacing"
      contains: "marginBottom: 8"
  key_links:
    - from: "titleText style"
      to: "authorText style"
      via: "marginBottom on titleText + marginTop on authorText"
      pattern: "marginBottom.*[89]|marginTop.*[23]"
---

<objective>
Fix the cover page text overlap reported in UAT test 4: the 22pt screenplay title descends into the 11pt author name because `titleText.marginBottom` is only 3pt — far too small for the font size.

Purpose: The coverage PDF is the formal deliverable sent to partners. Overlapping text on the cover page makes it look broken and unprofessional.
Output: A patched `CoverageDocument.tsx` where the title/author gap is visually comfortable (no overlap under any title length).
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-export-coverage-package/07-01-SUMMARY.md

<interfaces>
<!-- Relevant style object from CoverageDocument.tsx (lines 124-135) — executor edits these values directly. -->

```typescript
// ── Title ──
titleBlock: { marginBottom: 16 },
titleText: {
  fontSize: 22,
  fontFamily: 'Helvetica-Bold',
  color: C.grey900,
  marginBottom: 3,       // BUG: 3pt is far too small for a 22pt heading
},
authorText: {
  fontSize: 11,
  color: C.grey700,
  // no marginTop — entire gap is solely the 3pt on titleText
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix title-to-author spacing in CoverageDocument.tsx</name>
  <files>src/components/export/CoverageDocument.tsx</files>
  <action>
    Open `src/components/export/CoverageDocument.tsx`.

    In the StyleSheet definition, find the `titleText` style object (around line 126). Change `marginBottom` from `3` to `8`.

    ```diff
    -  marginBottom: 3,
    +  marginBottom: 8,
    ```

    Then find the `authorText` style object (around line 132). Add `marginTop: 2` as a defensive secondary gap in case any renderer clips marginBottom:

    ```diff
     authorText: {
       fontSize: 11,
       color: C.grey700,
    +  marginTop: 2,
     },
    ```

    No other changes. Do not touch font sizes, colors, titleBlock, or any other style objects.

    Rationale: A 22pt headline with lineHeight 1.4 renders ~30.8pt tall. The 3pt marginBottom left only 3pt of air before the 11pt author line, causing visual overlap. 8pt marginBottom + 2pt marginTop gives 10pt total breathing room — appropriate for this heading/body font-size pairing.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/LEMON-SCREENPLAY-DASHBOARD && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npm run build` exits 0. The two changed values are `marginBottom: 8` on `titleText` and `marginTop: 2` on `authorText`. No other lines in CoverageDocument.tsx are modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual spot-check — cover page title/author spacing</name>
  <action>Human verifies the patched PDF cover page looks correct.</action>
  <what-built>
    Patched `CoverageDocument.tsx` with corrected cover page title/author spacing. Build passes. Ready for visual spot-check.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (port 3000 — kill any existing dev server first).
    2. Open any screenplay detail modal.
    3. Click the "Coverage" button and download the PDF.
    4. Open the PDF and look at the cover page.
    5. Confirm: the screenplay title and author name below it are clearly separated with no descenders from the title touching the author line.
    6. If you have a screenplay with a long multi-word title, check that one too — worst case for line wrapping.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual PDF rendering requires human eyes</automated>
  </verify>
  <done>User confirms title and author name are visually separated with no overlap on the cover page.</done>
  <resume-signal>Type "approved" if the cover page looks correct, or describe any remaining issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (TypeScript strict, no regressions)
- `npm run test:run` exits 0 (no existing tests broken)
- Cover page PDF spot-check: title and author name visually separated with no overlap
</verification>

<success_criteria>
- UAT test 4 passes: cover page shows screenplay title and author name without text overlap
- `marginBottom` on `titleText` is 8 (was 3)
- `marginTop: 2` is present on `authorText`
- No other style changes in CoverageDocument.tsx
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-coverage-package/07-03-SUMMARY.md`
</output>
