# Q0 Trust-Hardening Baseline

Date: 2026-07-29

Branch: `codex/v9-trust-hardening`

Source checkpoint: `4620d6a4dcf7a63d88c7c3e14db534d46832901c`

## Purpose

Q0 preserves the current Cinema Browse experience and records the system as it
exists before any trust-hardening changes. It does not change analysis logic,
stored screenplay data, security rules, Functions, the VPS daemon, or
production hosting.

## Preservation checkpoint

The current Cinema Browse implementation was committed on
`codex/discovery-cinema-browse` before this branch was created.

- Commit: `4620d6a q0: checkpoint Cinema Browse before trust hardening`
- Build: passed
- Targeted Discovery tests: 16 passed
- Separate editorial fallback remains at
  `codex/discovery-editorial-fallback` commit `d849142`
- Unrelated screenshots, browser captures, generated concept directions, and
  the untracked root `AGENTS.md` were not added to the checkpoint

## Current analysis configuration

These values are descriptive only. Q0 does not approve them as the desired
future configuration.

### Models

| Internal tier | Exact model ID |
|---|---|
| Haiku | `claude-haiku-4-5-20251001` |
| Sonnet | `claude-sonnet-4-6` |
| Opus | `claude-opus-4-7` |

`auto` currently selects Sonnet. `hybrid` runs Sonnet first and promotes only
`RECOMMEND` and `FILM_NOW` results to Opus.

### Reader design

| Reader | Weight |
|---|---:|
| Structure | 30% |
| Character | 30% |
| Craft and Scene | 15% |
| Concept | 15% |
| Emotional Resonance | 10% |

- Maximum screenplay input: 195,000 characters
- Default evaluation temperature: 0.1
- Reader reasoning budget: 8,000 tokens
- Synthesis reasoning budget: 16,000 tokens
- Reader output budget: 4,000 tokens
- Synthesis output budget: 6,000 tokens

### Verdict calculation

| Adjusted score | Tier |
|---:|---|
| 8.5 or higher | `FILM_NOW` |
| 7.5 to 8.49 | `RECOMMEND` |
| 5.5 to 7.49 | `CONSIDER` |
| Below 5.5 | `PASS` |

The code subtracts critical-failure penalties before assigning the tier:

- Minor: 0.3
- Moderate: 0.5
- Major: 0.8
- Critical: 1.2
- Maximum combined penalty: 3.0

The Story-versus-Situation gate, false-positive trap gate, and truncation gate
can cap or downgrade the final verdict.

Boundary reruns trigger when the adjusted score is within 0.5 points of a tier
boundary. The current maximum is three complete runs.

## Executable baseline

| Check | Result |
|---|---|
| Production build | Passed |
| Lint | Passed |
| Dashboard unit/integration tests | 667 passed |
| Functions build and tests | 26 passed |
| Python tests through `ingest/.venv` | 93 passed |
| OCR fixture | Passed, 2 pages and 1,208 words extracted |
| Rules tests | Not rerun, local Java runtime is missing |

### Environment

- Node: 22.22.3
- npm: 10.9.8
- Project Python: 3.14.6

The system Python test invocation failed because it does not contain PyPDF2.
The repository's intended environment at `ingest/.venv` passed all 93 tests.

The rules-test command reached Firebase tooling but stopped before starting the
emulators because this Mac has no Java runtime. No dependency or system runtime
was installed during Q0. The earlier project handoff reports 20 rules tests,
but Q0 does not represent that as a fresh rerun.

## Baseline warnings

1. Vitest reports that one `vi.mock` call in `BulkShareModal.test.tsx` is not at
   module top level. It still passes today but is expected to become an error in
   a future Vitest version.
2. The production build reports several existing bundles above Vite's 500 kB
   warning threshold.
3. Python tests generate tracked bytecode changes in two `__pycache__` files.
   Q0 restores those generated changes before committing its evidence package.
4. The rules suite needs a project-approved Java runtime before it can be
   independently rerun on this Mac.

## Q0 invariants

- No screenplay document was written, updated, migrated, or deleted.
- No analysis was run against a paid model.
- No model ID, prompt, score, gate, or verdict was changed.
- No Function or rule was changed or deployed.
- No hosting deploy occurred.
- No VPS command was issued.
